{
	"toolchain_linux_arc64": {
		"execute": "$env(ROOT)/toolchain/arc-gnu-toolchain/configure --prefix=$env(PREFIX) --target=arc64 --enable-linux ; make -j32",
		"module_template" : "toolchain.module.erb"
	},
	"toolchain_baremetal_arc64": {
       		"execute": "$env(ROOT)/toolchain/arc-gnu-toolchain/configure --prefix=$env(PREFIX) --target=arc64 --enable-linux ; make -j32",
		"module_template": "toolchain.module.erb"
	}
}
